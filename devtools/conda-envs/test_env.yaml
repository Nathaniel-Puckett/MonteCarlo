name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    #Package dependancies
  - numpy
  - matplotlib
  - networkx
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

